# Mac OS X

# Colorize ls output
if brew help > /dev/null 2>&1 && brew ls --versions coreutils > /dev/null; then
    alias ls='ls --color=auto'
else
    alias ls='ls -G'
fi

# Shorthand for htop
if brew help > /dev/null 2>&1 && brew ls --versions htop-osx > /dev/null; then
    alias htop='sudo htop'
fi
